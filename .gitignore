node_modules/
backend/node_modules/
frontend/node_modules/
frontend/build/
backend/uploads/
.env
backend/.env
frontend/.env
*.log

# Local tooling config and historical DB backups — not app source, shouldn't ship
# (backend/src/config/*.sql are real source files — schema/seed for fresh installs — and are NOT excluded)
.claude/
exports/
/certifypro.sql
/certifypro_postgres.sql

# MySQL credentials file (plaintext password) — never commit
backend/my_login.cnf

# Tesseract.js OCR language model — downloaded automatically at runtime,
# not source; re-fetched on first use in any environment
backend/eng.traineddata
*.traineddata
